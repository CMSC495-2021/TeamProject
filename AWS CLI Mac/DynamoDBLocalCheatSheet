***Setup and Test Commands for DynamoDB***

# Must have AWS local profile setup FIRST

aws configure

$AWS Access Key ID [***]: for local make one up "MyAccessKey"
$AWS Secret Access Key [***]: same... "MySecretAccessKey"
$Default region name [***]: same... "east"
$Default output format [***]: I used "json"

# Starting a local DynamoDB Session

# This starts a session that you can't ctrl-c out of. To work with the session, make a new terminal window (mac)
dynamodb_local_latest % aws dynamodb list-tables --endpoint-url http://localhost:8000

#Working with DynamoDB Session

# DynamoDB commands are now used with the exception that you need to point them at local host by appending
# "--endpoint-url http://localhost:8000"...

#Test Local DynamoDB Session
aws dynamodb list-tables --endpoint-url http://localhost:8000

# Expected Output

{
    "TableNames": []
}

# Create Table

aws dynamodb create-table \
    --endpoint-url http://localhost:8000 \
    --table-name TestTable \
    --attribute-definitions \
        AttributeName=Message,AttributeType=S \
    --key-schema  \
        AttributeName=Message,KeyType=HASH \
    --provisioned-throughput ReadCapacityUnits=1,WriteCapacityUnits=1
